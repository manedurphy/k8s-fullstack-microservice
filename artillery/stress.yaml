config:
  target: "http://localhost:3000"
  phases:
  - duration: 60
    arrivalRate: 450
  payload:
    skipHeader: true
    path: "ids.csv"
    fields:
      - "space"
scenarios:
  - flow:
    - get:
        url: "/buildings/{{ space }}"
